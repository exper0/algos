package org.github.exper0.subsetsum;

import java.util.Collection;

/**
 * Iterable that provides ability to iterate over all subset sums using arbitrary object type with integer property.
 * @param <T> Element type
 * @author exper0 (aeg.exper0@gmail.com)
 */
public interface SubsetSumIterable<T> extends Iterable<Collection<T>> {
}
